# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - m2mclothingtanloc

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://m2mcolthing.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_e8740b182db74bcab072ac468e5d3395 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_9e32d82818994e7fb03cca5df6462f70 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: m2mcolthing.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_e8740b182db74bcab072ac468e5d3395 }}/m2mcolthing:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'm2mclothingtanloc'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_b0291d4072be4ec4b25b1821b6039e35 }}
        images: 'm2mcolthing.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_e8740b182db74bcab072ac468e5d3395 }}/m2mcolthing:${{ github.sha }}'